import type { Config } from 'vike/types'

export const config = {
  title: 'Changing the Root of a Tree | Linguistics Tree Solver',
  description:
    'By default, the Linguistics Tree Solver builds trees from a full sentence, but not every sentence is a complete one. This guide will teach you how to change the root of a syntax tree to build trees for clauses in a sentence.'
} satisfies Config
